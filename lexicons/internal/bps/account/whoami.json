{
  "lexicon": 1,
  "id": "internal.bps.account.whoami",
  "defs": {
    "main": {
      "type": "query",
      "description": "Return the logged-in account's DID, handle, stored email, and whether an email is set.",
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["did", "hasEmail"],
          "properties": {
            "did": { "type": "string", "format": "did" },
            "handle": { "type": "string" },
            "email": { "type": "string" },
            "hasEmail": { "type": "boolean" }
          }
        }
      }
    }
  }
}
