{
  "lexicon": 1,
  "id": "internal.bps.health",
  "defs": {
    "main": {
      "type": "query",
      "description": "Liveness/readiness probe for the account backend.",
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["status", "db"],
          "properties": {
            "status": { "type": "string", "const": "ok" },
            "db": { "type": "boolean" }
          }
        }
      }
    }
  }
}
