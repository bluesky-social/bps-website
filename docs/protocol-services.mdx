---
slug: /protocol-services
title: Get Started
sidebar_label: Get Started
hide_title: true
description: Bluesky runs the public infrastructure of the AT Protocol — Jetstream, the Relay, and the app API — as open services you can build on today, no permission required. Start with Jetstream.
---

import DocsHero from '@site/src/components/DocsHero';
import DocsCardGrid from '@site/src/components/DocsCardGrid';
import JetstreamLive from '@site/src/components/JetstreamLive';
import DocItemTOCMobile from '@theme/DocItem/TOC/Mobile';

<DocsHero eyebrow="Get Started" title="Explore the network">
  Bluesky operates public infrastructure for AT Protocol as open services anyone can build on.
</DocsHero>

<DocItemTOCMobile />

<br/>

Bluesky runs on a public social network of users' own individual data repositories. This is the [AT Protocol](https://atproto.com). Anyone can build on the AT Protocol with the data already on the network.

**Many AT Protocol services and infrastructure are provided by Bluesky.** They sit at different
layers, from a jetstream of everything happening right now, down to the
cryptographically verifiable raw firehose; but they all share the same open data
and the same open access. These docs describe the public protocol services and show how to build on them.

**Jetstream** is the fastest way to stream and query real network data.

## Start with Jetstream

On AT Protocol you can access all the public data on Bluesky; posts, likes, and the full Bluesky social graph. But you can also tap into other kinds of data, like [shared music samples and mixes](https://freemix.fm/), [a growing index of blog posts across the web](https://standard-reader.app/), and [local community groups](https://gifthood.community/).

Jetstream hands you just the slice of the network you want as plain JSON over a WebSocket. The stream below is the live
network, right now:

<JetstreamLive ctaHref="/docs/jetstream" ctaLabel="Get started with Jetstream" />

That's the same connection you'll write in the [Jetstream
docs](/docs/jetstream): a few lines, and you're consuming the network live or
replaying it from history.

## The protocol services

<DocsCardGrid
  items={[
    { title: 'Jetstream →', body: 'Filtered, decoded JSON over one WebSocket. Live, historical, or both. The place to start.', to: '/docs/jetstream' },
    { title: 'The Relay', body: 'The full firehose in its raw, cryptographically verifiable form — every record, binary-encoded.', to: '/docs/relay' },
    { title: 'HTTP Reference', body: 'Bluesky app endpoints: profiles, posts, threads, feeds, and interactions over HTTP.', to: 'https://endpoints.bsky.app' },
    { title: 'Bluesky API', body: 'Bluesky API docs.', to: '/docs/bluesky-api' },
  ]}
/>

Use the **Relay** when you need every record with no filtering, or the
proofs to verify repository state yourself. Use the **Bluesky API** when you're
building against the Bluesky app's data model directly — see [API hosts and
auth](/docs/api-directory) for endpoints and authentication, or browse the [full
HTTP reference](https://endpoints.bsky.app).

## Building on the Bluesky app

If you're looking to develop for the [Bluesky app](https://bsky.app) — making your first post,
reading feeds, working with profiles and interactions — the [Bluesky
API](/docs/bluesky-api) guides walk through the API step by step.
